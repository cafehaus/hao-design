import HModal from './components/h-modal'
import HPager from './components/h-pager'
import HCollapse from './components/h-collapse'
import HDownloadExcel from './components/h-download-excel'
import HPoptip from './components/h-poptip'
import HPreviewImg from './components/h-preview-img'
import HRadioEnable from './components/h-radio-enable'
const components = {
    HModal,
    HPager,
    HCollapse,
    HDownloadExcel,
    HPoptip,
    HPreviewImg,
    HRadioEnable,
}

// 单个导出：可按需引入 import { test } from 'hao-design'
export {
    HModal,
    HPager,
    HCollapse,
    HDownloadExcel,
    HPoptip,
    HPreviewImg,
    HRadioEnable,
}

// 整体导出
export default components